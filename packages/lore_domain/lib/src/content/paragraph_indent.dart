/// 中文段落首行缩进：两个全角空格（U+3000 × 2）。
///
/// 编辑器（首行缩进样式、回车开新段、加载兜底）与导入解析（段首归一化）共用
/// 的单一真值。仅在此处定义，其余包 import 引用——避免各处各自写 `'　　'`
/// 导致漂移（如一处改成 4 个全角空格、另一处仍是 2 个，缩进就不一致）。
const String paragraphIndent = '　　';
