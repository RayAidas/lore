name: lore_platform_adapters
description: Flutter platform and device-local adapters for Lore.
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.6.0
  lore_application:
    path: ../lore_application
  lore_domain:
    path: ../lore_domain
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_secure_storage_platform_interface: ^1.1.2
  flutter_test:
    sdk: flutter

flutter:
